describe('axioms', function() {
    it('Math works', function() {
        expect(1+2).toBe(3);
    });
    xit("Should be skipped", function() {
        expect("this").toEqual("not this");
    });
});